# Snapshot binaries live as release assets — see manifest.json + scripts/download-snapshot.sh.
# Treat them as binary if they ever appear locally; do not LFS-track.
*.tar.zst.part* binary
*.tar.zst binary
